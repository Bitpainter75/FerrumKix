<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:icons="clr-namespace:FerrumPlay.Controls"
             x:Class="FerrumPlay.Views.ConverterPanel">
  <Grid RowDefinitions="Auto,*,Auto" Background="{DynamicResource FP.Bg.Content}">
    <Grid Grid.Row="0" ColumnDefinitions="Auto,*,Auto" Margin="18,14,14,10">
      <Button Grid.Column="0" Classes="icon-btn" Click="OnBackClick" ToolTip.Tip="Zurück zur Wiedergabeliste">
        <icons:SvgIcon Source="avares://FerrumPlay/Assets/Icons/outline/chevron-left.svg" Width="17" Height="17"/>
      </Button>
      <StackPanel Grid.Column="1" Margin="12,0" VerticalAlignment="Center">
        <TextBlock Text="Audio konvertieren" FontSize="{DynamicResource FP.Font.Heading}" FontWeight="SemiBold"/>
      </StackPanel>
      <TextBlock Grid.Column="2" x:Name="CountText" Classes="secondary" VerticalAlignment="Center"/>
    </Grid>

    <Grid Grid.Row="1" RowDefinitions="*,Auto" Margin="18,0,18,12" RowSpacing="14">
      <Border BorderBrush="{DynamicResource FP.Border.Normal}" BorderThickness="1" CornerRadius="7" ClipToBounds="True">
        <ListBox x:Name="QueueBox" ItemsSource="{Binding Queue}" BorderThickness="0">
          <ListBox.ItemTemplate>
            <DataTemplate>
              <Grid ColumnDefinitions="*,Auto" Margin="10,6">
                <StackPanel>
                  <TextBlock Text="{Binding Title}" TextTrimming="CharacterEllipsis"/>
                  <TextBlock Text="{Binding Detail}" Classes="muted" TextTrimming="CharacterEllipsis"/>
                </StackPanel>
                <TextBlock Grid.Column="1" Text="{Binding Status}" Classes="secondary" VerticalAlignment="Center" Margin="12,0,0,0"/>
              </Grid>
            </DataTemplate>
          </ListBox.ItemTemplate>
        </ListBox>
      </Border>

      <Border Grid.Row="1" Background="{DynamicResource FP.Bg.Panel}" BorderBrush="{DynamicResource FP.Border.Normal}" BorderThickness="1" CornerRadius="7" Padding="12">
        <StackPanel Spacing="10">
          <WrapPanel>
            <TextBlock Text="Zielformat" Classes="section-header" Width="108" VerticalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" Spacing="10">
              <RadioButton x:Name="Mp3Radio" GroupName="format" Content="MP3" IsChecked="True"/>
              <RadioButton x:Name="FlacRadio" GroupName="format" Content="FLAC"/>
              <RadioButton x:Name="OggRadio" GroupName="format" Content="Ogg"/>
            </StackPanel>
          </WrapPanel>
          <WrapPanel>
            <TextBlock Text="MP3-Qualität" Classes="section-header" Width="108" VerticalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" Spacing="10">
              <RadioButton x:Name="CbrRadio" GroupName="bitrate" Content="CBR" IsChecked="True"/>
              <RadioButton x:Name="VbrRadio" GroupName="bitrate" Content="VBR"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Spacing="8" Margin="14,0,0,0">
              <RadioButton x:Name="B128Radio" GroupName="rate" Content="128"/>
              <RadioButton x:Name="B192Radio" GroupName="rate" Content="192" IsChecked="True"/>
              <RadioButton x:Name="B256Radio" GroupName="rate" Content="256"/>
              <RadioButton x:Name="B320Radio" GroupName="rate" Content="320"/>
            </StackPanel>
          </WrapPanel>
          <Grid ColumnDefinitions="108,*">
            <TextBlock Text="Ausgabe" Classes="section-header" VerticalAlignment="Center"/>
            <ComboBox x:Name="ModeBox" Grid.Column="1" SelectedIndex="0">
              <ComboBoxItem Content="Ein Ergebnis je Titel"/>
              <ComboBoxItem Content="Gesamtauswahl zusammenführen"/>
              <ComboBoxItem Content="Je Ordner zusammenführen"/>
            </ComboBox>
          </Grid>
          <WrapPanel HorizontalAlignment="Left" ItemHeight="48">
            <TextBox x:Name="FolderBox" PlaceholderText="Zielordner auswählen" IsReadOnly="True" Width="420" VerticalAlignment="Center"/>
            <Button Content="Ordner…" Click="OnChooseFolderClick" Margin="8,0,0,0" VerticalAlignment="Center"/>
            <Button x:Name="ConvertButton" Content="Konvertierung starten" Click="OnConvertClick" Margin="8,0,0,0" VerticalAlignment="Center"/>
          </WrapPanel>
        </StackPanel>
      </Border>
    </Grid>
    <Border Grid.Row="2" Background="{DynamicResource FP.Footer.Bg}" Padding="18,7">
      <Grid ColumnDefinitions="*,Auto" ColumnSpacing="12">
        <TextBlock x:Name="StatusText" Classes="secondary" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
        <ProgressBar Grid.Column="1" x:Name="Progress" Width="130" Height="5" IsIndeterminate="True" IsVisible="False" VerticalAlignment="Center"/>
      </Grid>
    </Border>
  </Grid>
</UserControl>
